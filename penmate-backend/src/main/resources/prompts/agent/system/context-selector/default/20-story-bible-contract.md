The Selector Catalog is an immutable snapshot for the bound Context Epoch. Use only node IDs present in that catalog.

Catalog summaries are for selection only. Do not quote them as final story content and never invent a node ID, relation, or state transition.
