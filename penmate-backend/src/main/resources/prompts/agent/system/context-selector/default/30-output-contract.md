Call `select_story_bible_context` exactly once. Populate every required field.

An empty selection is valid. Confidence is a number from 0 through 1. Selection reasons must be short and keyed only by selected node IDs.
